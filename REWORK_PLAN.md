# 사이트 세대 교체 설계 (rework/v4-anchor) — 2026-08-24 착수

> 2026-08-31 개정: **간척 아틀라스 탭(atlas.html) 폐지 → 시행령 제정안 영향 탭(decree.html)으로
> 대체.** 근거: 영농형태양광법 시행령 제정안(의견조회판, 2026.7) §7 6호 — 농업회사법인 부지를
> 농어촌정비법 §14① 매립지등(매각분 제외 = 국가관리간척지)으로 한정. 수치는
> pipeline/export/export_decree_v4.py(정본 query.py matrix, 모집단 '국가관리간척지') →
> data_v4/decree_v4.json 렌더. 구 아틀라스 자산(assets/atlas_sat_v4.html·korea_owner_v4.html)은
> 참조만 끊고 파일은 이력 보존 — 매각 지구(대호 등)가 '가능 지역'으로 보일 수 있어 재사용하지 않음.

목표: 2026-07-22 세대(21개 구역·MW 1차·구 S0/S1/S2)를 **앵커 개정판 + 조합 격자 +
두 수단 스토리** 세대로 전면 교체. 무효·동결 수치의 노출 0.

## 원칙

### A. 수치 — 무엇을 싣나

1. **단일 출처**: 페이지 본문에 수치 하드코딩 금지 — 모든 수치는 data_v4 JSON에서만
   렌더한다. JSON은 정본 산출(query.py·scenario_runs·검증 통과분)의 export만 —
   사이트에서 재계산·수기 입력 금지. (한 곳을 고치면 전체가 갱신되고, 브리프와
   사이트가 어긋날 수 없는 구조)
2. **기준선 명시**: 모든 수치 곁에 기준선을 붙인다 — 실경작 전/후, 필지층/구획층,
   앵커 대비 Δ. 참고 MW는 가정(ρ 등)을 함께 표기.
3. **무효·구세대 노출 0**: 구 이격 3종, 초판 앵커 5,039.81, 판독률=휴경 계열
   (16.2%·6,755MW), 동결 grid_pool, 구 S0/S1/S2 축. 재도출 전 값 인용 금지
   (확정 게이트 승계).
4. **하한·공백 명시**: HARD=하한(미분해 17종), 계통 589동 원천 NULL 등 불확실성은
   숨기지 않고 본문에 병기한다.

### B. 표현 — 어떻게 싣나

5. **결과 제시 방침 승계**: 평균 개념 금지 · 비중(%)보다 실측값 · km² 통일
   (면적 1차, MW 참고 병기 — 구 사이트의 MW 1차를 전면 치환) · 시군별 실측값이 1차.
6. **용어 방침 승계**: 법문·공식 표현 우선(교정표 적용), 은유 금지, 중립 제목
   (대립 구도 제목 지양), 물결표 금지(범위는 –).
7. **시나리오 = 독립 조합**: 누적 사다리 서사·그림 금지 — 보호구역만/진흥구역만/
   둘 다를 병렬로, 각각 실경작 전·후 두 값.
8. **그림은 FIGURE_RULES(ADR-0033)**: 장르 선언, 결론장 상한(강조 숫자 ≤4 등),
   배포 전 3테스트. 각주는 브리프와 공통 번호 체계.

### C. 공개물 — 웹이라서 생기는 의무

9. **개인정보 최소화**: 소유는 지적 원장의 유형 구분까지만("개인 식별 아님" 상시
   병기). 필지 단위 소유 레이어의 공개는 **사용자 승인 완료(2026-08-25)** —
   지적공부는 본래 공개 정보이고 유형 구분은 개인 식별이 아니라는 판단.
10. **자족형**: 외부 CDN·외부 API 의존 금지(타일 내장·정적 JSON) — GitHub Pages에서
    추가 설정 없이 동작. (파일 패널 "L is not defined" 사례의 재발 방지)
    예외(2026-08-25 개정): ①웹폰트는 Google Fonts 허용 — 단 시스템 폴백 필수,
    ②대화형 지도 페이지의 배경 타일은 온라인 허용(오프라인 대체 = 내장 아틀라스),
    ③라이브러리(Leaflet 등)는 CDN 금지 — 저장소에 내장(vendored).
11. **계보 표기**: 각 페이지 푸터에 데이터 세대(v4)·생성 시각·검증 상태(시금석
    통과)·판 번호(1판/2판). 외부 자료 라이선스(위성 타일 등) 명기.
12. **push = 공개 행위**: 사용자 승인 후에만. 승인 전 로컬 시연 필수.
    로컬 커밋은 자유.

### D. 집행 — 규칙은 문서보다 코드로

13. **빌드 게이트(pipeline/site_gate.py)**: 기계 검사 가능한 원칙은 게이트로 강제,
    시연·push 전 필수 통과.
    - ① 무효 수치 스캔 (원칙 3의 금지 수치 목록)
    - ② 금지어·물결표 스캔 (평균, 교정표 위반어, ~)
    - ③ data_v4 ↔ 정본 DB 시금석 대조 (앵커 T14 등 대표값 재조회 일치)
    - ④ 외부 URL 스캔 (CDN·외부 스크립트 참조 검출)
    - ⑤ 하드코딩 수치 탐지 (페이지 내 큰 숫자가 data_v4에 없으면 경고)
    - 새 함정 발견 시: 문서에 적기 전에 게이트에 검사부터 추가.

## 확정 사항 (2026-08-24 사용자 결정)

- **독자**: 첫 화면은 정책 스토리(결론 1–3), method/about에 재현 체계를 깊게 (의제1 c안)
- **페이지 축소**: 7개 → 5개 — index(결론) / evidence(매트릭스·소유·지도) / atlas(간척) /
  method(방법·재현, 이격 상한제 절 흡수) / about(계보·한계)
- **구 21구역 아틀라스**: 참조 끊고 파일만 이력 보존 (구세대 수치 박힌 이미지 — 노출 차단)
- **공개 단위**: 필지층 확정값으로 1차 완성 → 시연 → 1판 격자 반영 2차
- **영어판**: 한국어 확정 후 일괄, 1차 공개는 KO만
- **지도**: 자족형(타일 내장·정적 JSON). 지구명은 지도 위 텍스트 대신
  **번호 동그라미 마커 + 지도 아래 번호별 지구명·설명** — 작은 글씨 라벨 금지 (가독성)

## 디자인·모듈 승계 (2026-08-25 사용자 지시 — "시각화 제대로 살려서, 디자인도 중요")

구판(복원점 05a3a3a, 로컬 사본 C:\Users\user\새 폴더\Agrivoltaic-Cluster)의
PLANiT 디자인 체계와 시각화 모듈을 v4 데이터로 되살린다. 뼈대 CSS를 새로 쓰지 않는다.

| 모듈 | 내용 | 시점 |
|---|---|---|
| style.css (37KB) | PLANiT 디자인 시스템 전면 승계 — 네이비·골드·리프그린, 세리프 표제, eyebrow, statband(카운트업), 카드 호버, reveal | **지금** |
| site.js | 스크롤 리빌 · 숫자 카운트업 · KO/EN i18n 엔진 (data-i18n) | 지금 (EN 사전은 후속) |
| 페이지 chrome | topbar + 로고 내비 + lang-toggle + Contact + 네이비 푸터 통일 | 지금 |
| 시군 진단 도구 | 전국 시군 select → R0–R3 카드 4장 + 자동 VERDICT — sgg_matrix.json으로 즉시 구동 | 지금 |
| 3D 레이어 스테이지 | 필지 폴리곤 판(判)별 누적 시연 + 깔때기 연동 — **1판 구획 geojson으로 재생** (구 데이터는 구세대 조건이라 인용 불가) | 1판 후 |
| Leaflet 지도 앱 | map_full_v3 (시군 선택·단계구분도·블록·후보 순위) — Leaflet 내장(vendored) + 1판 구획 | 1판 후 |
| appendix.js | 용어집·FAQ 모듈 | EN과 함께 후속 |

시군 이름은 구판 summary.json의 코드→이름 매핑만 재사용(행정 표준명 — 수치 아님).

## 데이터 세대 교체 (data_v4/)

| 신규 파일 | 내용 | 원천 | 시점 |
|---|---|---|---|
| summary_v4.json | 앵커(전국+시도) · 조합 매트릭스 8칸(전국·간척) · 세 무리 비교 | 정본 DB 조회(브리프와 동일 질의) | **지금** |
| grid_cells.json | 격자 12칸+민감도 (필지·구획 2층, Δ) | scenario_runs | 1판 후 |
| sgg_anchor.json | 시군별 앵커·A1·A2 (지도 채색용) | 정본 DB | 지금 |
| reclaim_atlas.json | 간척 4시군 필지(소유·실경작) + 지구 통계 | 정본 DB×지적 | 지금 (도식판 이식) |
| clusters_v4/*.json | 시나리오별 등재 구획 geojson (지도) | 엔진 members+지오메트리 | 1판 후 |
| meta_v4.json | 자산 계보·검증 상태·생성 시각 | meta_versions·touchstones | 지금 |

구 data/·data_v3/는 삭제하지 않고 유지(이력) — 페이지 참조만 끊는다.

## 페이지 매핑 (구 → 신)

| 페이지 | 현재 (구세대) | 교체 내용 |
|---|---|---|
| index | 21구역 MW 요약 | **결론 1**: 두 수단(부령 보호구역·재생에너지지구), 앵커 4,099.64km², 깔때기 요약 |
| insight | 구 시나리오 해설 | 조합 매트릭스(4×2) · 조문 검토 5막 스토리 · 결론 2(소유)·결론 3(세 무리 지도+표) |
| atlas | 21구역 이미지 아틀라스 | **간척 위성 아틀라스**(4시군, 소유 구분·지구 라벨) + 발표 대본 요지 |
| map/map_full | 구 클러스터 지도 | 시군 채색(앵커·A1·A2 증분) → 1판 후 구획 geojson 겹침 |
| method | 구 방법론 | 방법장 깔때기 + 판정 조건표 + 재현 체계(시금석 53·게이트·agv) |
| ordinance | 무효 이격 실측(레거시 브랜치行) | **이격 상한제 페이지**: §27조의3·200m 상한·2판 병기 원칙 |
| about | 구 소개 | 데이터 계보(L0–L3)·출처 각주 16종·한계 5항(브리프 §5) |

## 작업 순서

1. ✅ 상태 정리: 무효 이격 WIP → legacy/setback-invalid-20260810 보존, main=origin/main
2. **지금 (필지층 확정값으로)**: data_v4 export 스크립트(pipeline/export_v4.py — 정본 DB 읽기 전용)
   → index·insight·method·ordinance·about 콘텐츠 교체 → 도식 5종 이식
3. **1판 완주 후**: grid_cells·clusters_v4 export → map·atlas 완성 → 격자·강건성 절 반영
4. i18n(EN): 한국어 확정 후 일괄 (구 fdb5f09 방식 승계)
5. 검수: site_gate 5종 검사(원칙 13) 통과 → 필지 소유 레이어 공개 여부 확인(원칙 9)
   → 사용자 시연 → push 승인 별도

## 이 브랜치에서 하지 않는 것

- 원격 push (사용자 승인 사안) · 구 데이터 삭제 · 이격 실측 콘텐츠 복원(무효)
